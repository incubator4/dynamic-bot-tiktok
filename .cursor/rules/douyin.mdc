---
description: 抖音接入必须保守：Cookie 登录、限速、风控停手、不落盘密钥
alwaysApply: true
---

# 抖音接入

平台标识固定为 `tiktok`。对外文案用「抖音」。

- 一期支持 Cookie 与扫码登录；不要把真实 Cookie、抓包 HAR、会话文件提交进仓库。
- 默认关闭轮询或使用不少于 60s 的间隔；连续请求间隔不少于 1s。
- 未登录、Cookie 失效、疑似风控：暂停轮询，不要加密重试或绕过校验。
- 启动默认不补发旧作品（补发窗口默认 0）。
- 订阅键用抖音用户 ID；不要默认扫全站或对每个用户高频打个人接口。
- 直播只检查订阅了开播/下播事件的用户；启动先记当前状态，不把已在播当成新开播。
- 文档和代码只写本插件如何对接 dynamic-bot，不写利用平台漏洞或未授权访问的步骤。
- 链接解析（若实现）覆盖作品、用户主页和 `v.douyin.com` 短链。直链只按 URL 生成本地预览，不请求需 Cookie 的详情；短链无法本地解析时返回可理解的中文原因。
- 一期不覆盖国际版 TikTok（`tiktok.com`）。
